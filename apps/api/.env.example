# OpenAI API key — required from Phase 3 for the prompt router.
# Without it the router still responds, using its fallback classification.
OPENAI_API_KEY=

# OpenAI model used for prompt classification. A small, fast model is fine.
OPENAI_MODEL=gpt-4o-mini

# Frontend origin for CORS
WEB_ORIGIN=http://localhost:3000
